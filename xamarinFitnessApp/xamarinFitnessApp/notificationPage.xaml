<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             x:Class="xamarinFitnessApp.notificationPage">
    <ContentPage.Content>
        <StackLayout Padding="20">

            <Label TextColor="White" Text="My Reminders!" FontSize="Large" HorizontalOptions="Center"></Label>
            <Label></Label>
            <Label TextColor="White">What do you need to remember?</Label>
            <Entry x:Name="txtToDo" TextColor="White"></Entry>
            <Label TextColor="White">Select when you need to be notified!</Label>
            <DatePicker TextColor="White" x:Name="datePicker" Date="{x:Static system:DateTime.Today}" />
            <TimePicker TextColor="White" x:Name="timePicker" Time="16:00:00" />
            <Button TextColor="White" StyleClass="Info" Text="Remind me!" Clicked="Button_Clicked" VerticalOptions="EndAndExpand" HorizontalOptions="Center"></Button>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>