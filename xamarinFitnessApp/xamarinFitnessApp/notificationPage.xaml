<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             x:Class="xamarinFitnessApp.notificationPage">
    <ContentPage.Content>
        <StackLayout x:Name="sl">
            <Label Text="Set up some notifications!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand"
                   TextColor="White"/>

            <Label TextColor="White">What do you need to remind?</Label>
            <Entry x:Name="txtToDo" TextColor="White"></Entry>
            <Button TextColor="White" Text="Remind me!" Clicked="Button_Clicked"></Button>
            <DatePicker x:Name="datePicker" Date="{x:Static system:DateTime.Today}" />
            <TimePicker x:Name="timePicker" Time="4:15:26" />
            <Entry x:Name="txtdayDiff" TextColor="White"></Entry>
            <Entry x:Name="txtTimeDiff" TextColor="White"></Entry>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>